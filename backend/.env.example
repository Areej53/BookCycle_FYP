MONGO_URI=mongodb://127.0.0.1:27017/your-db-name
JWT_SECRET=use-a-long-random-string
PORT=5000
# Must match VITE_PROXY_TARGET port in frontend/.env during local dev
# Optional: used in non-production forgot-password JSON (reset link origin)
FRONTEND_URL=http://localhost:5173
