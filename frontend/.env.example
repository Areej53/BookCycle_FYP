# Where Vite forwards /api during `npm run dev`
VITE_PROXY_TARGET=http://127.0.0.1:5000

# Optional: full API base URL (overrides proxy). Set for production builds when
# the UI and API are on different hosts.
# VITE_API_URL=https://your-api.com/api/v1
